import type { Metadata } from 'next';
import './globals.css';
import { Header } from '@/components/Header';
import { Footer } from '@/components/Footer';

export const metadata: Metadata = {
  title: 'SwiftCareerHub - Swift, SwiftUI & iOS Interview Prep',
  description: 'Learn Swift, SwiftUI, iOS architecture, and coding interview preparation with practical tutorials.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>
        <Header />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
